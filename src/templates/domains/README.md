# src/templates/domains

Domain-specific wiki templates (page scaffolds, frontmatter presets) loaded by the
generator. Empty by default — the built-in `default` template under
`templates/default/` is used until domain templates are added here.
