# templates/default/wiki

The seed `wiki/` directory copied into a new vault by `wikimem init`. Generated wiki
pages (sources/, entities/, concepts/, syntheses/) are written here at runtime; this
directory ships empty so a fresh vault starts clean.
