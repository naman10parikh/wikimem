---
type: plan
for: maintainer-prompt-079
date: 2026-04-23
status: active
author: CEO (Claude)
---

# WikiMem v1.0 Ship Plan — Maintainer the planning sprint

**Bar:** "holy shit, that's done." Not good enough. Not ready for launch. Investor-grade, end-to-end, zero dead buttons, zero console errors, one consistent UI language.

**Method:** Mix **the agent swarm** (persistent tmux panes with distinct personas — long-lived, stateful workers) with **sub-agents** (parallelizable short-lived research + surgical code changes). Spotify-queue model: follow-up bugs discovered by CEO go back to the SAME persona that's already working that surface — never broadcast. CEO (this thread) is the vibe tester — every code change gets a live browser verification within 60 seconds.

## Reality Check (North Star)

| Dimension                   | State as of 2026-04-23 22:57 PT                                                                                                                                                                        |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Product version             | v0.10.0 built, 100/100 tests, 591 kB tarball — **not yet on npm, not yet pushed to GitHub**                                                                                                            |
| Server                      | Running `wikimem serve` at localhost:3456 against `$HOME/test-wiki` (21 pages / 56 sources / 123 links)                                                                                         |
| Maintainer-reported P0 issues | (a) Observer rail button dead (b) Markdown pages "mix and match" of metadata cards                                                                                                                     |
| CEO-discovered P0 issues    | (a) Settings rail button also dead (same root cause) (b) 5 stacked metadata cards per entity page (c) View activation desyncs from tab state (d) Content doesn't use full viewport width               |
| Architecture gap            | MCP OAuth 2.1 exists as SERVER (v0.10.0) but connectors page still has 44 per-provider hardcoded OAuth flows — connectors should use MCP OAuth as CLIENT too (the Anthropic Claude Connectors pattern) |
| Documentation               | MASTER-TODOS.md §49 (this session) documents every bug verbosely with persona + severity + exact line numbers + fix surface                                                                            |

## Workstream Decomposition

Six parallel workstreams. Each has a **persona owner** (stable across the session) and a **queue** (follow-up bugs discovered during execution are appended to their queue, not broadcast). Personas map to the agent swarm panes + dedicated sub-agents.

### W1 — RAIL-WIRE (persona: SENTRY)

Fix broken icon-rail wiring + ensure every one of 9 rail icons switches views correctly AND syncs active-class state correctly.

- BUG-079-001: wire `#rail-observer` click → `railObserverClick` (line 22072 handler exists)
- BUG-079-002: wire `#rail-settings` click → `railSettingsClick` (line 19623 handler exists; currently attached only to `#sb-settings-btn`)
- BUG-079-003: verify active-state sync block at 12157–12180 updates for all 9 rails
- Follow-up bucket: BUG-079-060 (Observer view empty-rendered after open)

Assigned: sub-agent `adc3a19f759300d32` (running) + CEO verify.

### W2 — MARKDOWN-PAGE-OVERHAUL (persona: HERALD)

Consolidate the mix-and-match of info cards on every page into ONE clean reading experience (Obsidian parity). Make content take full viewport width. Fix the slug-title problem. Restore the v0.9.0 clean reading experience that regressed in v0.10.0.

- BUG-079-010 (duplicate Properties form + METADATA card)
- BUG-079-070 (5 redundant info cards on entity pages — METADATA + ENTITY + Mentioned in + FIRST ADDED/LAST MODIFIED/REFERENCED BY + CONTENTS)
- BUG-079-071 (content doesn't use full viewport width — massive dead space)
- BUG-079-072 (intro paragraph rendered as blue-bar blockquote, should be prose)
- BUG-079-073 (Mentioned-in items rendered as boxed buttons, should be inline wikilinks)
- BUG-079-074 (FIRST ADDED / LAST MODIFIED / REFERENCED BY is a 3rd redundant metadata card)
- BUG-079-075 (raw ISO timestamps rendered instead of humanized dates)
- BUG-079-076 (ENTITY infobox pill header redundant with breadcrumb entity name)
- BUG-079-077 (TOC positioned mid-page between metadata and content — should be sticky right-sidebar or collapsed header)
- BUG-079-078 (H1 rendered with numeric "1." prefix — likely ordered-list CSS regression)
- BUG-079-011 (page titles displayed as kebab-case slugs, not Title Case)
- BUG-079-012 (metadata label casing inconsistent — Type/Tags/Sources title-case vs category/stats lowercase)
- BUG-079-013 (Type="source" vs category="concept" semantic conflict)
- BUG-079-014 (breadcrumbs lack visible click affordance)
- BUG-079-015 (TOC missing on some pages pending verification)

**Scope of revert:** inspect `git log src/web/public/index.html` for changes between v0.9.0 (known good reading experience per maintainer) and v0.10.0 (current regressed state). Identify commits that added METADATA card, ENTITY card, "Mentioned in" boxed-links, FIRST ADDED/LAST MODIFIED/REFERENCED BY — REVERT those surface-layer additions while keeping the backend improvements (MCP OAuth, KARP patterns, Discord sync).

Assigned: new sub-agent (HERALD-1) — see "Swarm Spawn" below.

### W3 — CONNECTORS-MCP-OAUTH (persona: CONCIERGE)

Refactor connectors page from "44 hardcoded per-provider cards" to "MCP-OAuth-2.1 client — paste an MCP URL, auto-DCR + PKCE + Resource Indicator, user consents once." This is the Anthropic Claude Connectors pattern.

Architecture (per research at `$PROJECT_ROOT/resources/read/2026-04-17_mcp_oauth_briefing.md`):

1. **WikiMem UI** gains an "Add MCP Server" input on the Connectors page (paste URL).
2. **WikiMem MCP Client code** (new: `src/core/mcp-client/` — ~600 LOC):
   - Probes `POST <url>/mcp` → 401 → extract `WWW-Authenticate: resource_metadata="..."`
   - Fetches PRM (`.well-known/oauth-protected-resource`) → learns AS URL + scopes
   - Fetches ASM (`.well-known/oauth-authorization-server`) → learns auth + token + registration endpoints
   - Does DCR (`POST /register`) OR uses CIMD if supported OR falls back to user-pasted client_id
   - Generates PKCE (S256) + state + resource
   - Opens user's browser to `/authorize?...`
   - Catches callback at `http://localhost:3456/api/mcp-client/callback?...`
   - Exchanges code for `{access_token, refresh_token}`
   - Stores tokens in `TokenStore` keyed by MCP server URL
   - Uses `POST /mcp` with Bearer to enumerate tools, run `tools/call`
3. **Existing per-provider connectors** (Slack/Gmail/etc. at `src/core/sync/*.ts`) become OPTIONAL fallback — but the primary path is "MCP server URL." We ship a curated list of known MCP servers users can enable with one click (Slack's official MCP, GitHub's, etc., once they exist).
4. **Preserve existing wikimem-as-server** (v0.10.0) — two roles: wikimem IS an MCP server (for Claude to connect to) AND wikimem IS an MCP client (to connect to third-party MCP servers).

Bugs folded in:

- BUG-079-030 (search input truncated)
- BUG-079-031 (sidebar categories ≠ top category tabs)
- BUG-079-032 (Gmail Since-date breaks card grid)
- BUG-079-033 (duplicate "CONNECTORS" sidebar header)
- BUG-079-034 (per-card category mismatch)
- BUG-079-040 (MS Teams/Outlook/OneDrive stuck Coming Soon)
- BUG-079-041 (200-connector target from CP#77)
- BUG-079-080 (Slack Connect modal fires API but no UI response — from sub-agent ac8cca8f)
- BUG-079-081 (Discord Connect navigates instead of modal — from ac8cca8f)
- BUG-079-082 (Linear Connect unresponsive — from ac8cca8f)
- BUG-079-083 (Connectors grid shows "Loading…" too long — from ac8cca8f)
- **BUG-079-090 (NEW — MCP-OAuth-client rearchitecture)**: collapse 44 bespoke OAuth flows into one MCP OAuth 2.1 client. Cut LOC by ~70%. Give wikimem Anthropic-parity.

Assigned: new sub-agent (CONCIERGE-1) + stretch follow-up sub-agent (CONCIERGE-MCP-REARCH).

### W4 — VIEW-SWITCH-CORE (persona: NAVIGATOR)

Fix the `showView()` function so tab-activation, rail active-class, and visible view are always atomically in sync. Plus command palette + quick switcher + sidebar collapse keyboard shortcuts.

- BUG-079-020 (tab activation desync — visible view != active tab)
- BUG-079-021 (`openPage()` programmatic call doesn't render)
- BUG-079-050 (right-click context menu on sidebar tree)
- BUG-079-051 (single-click-to-edit gate)
- BUG-079-052 (Cmd+B / [ sidebar collapse shortcut verification)
- BUG-079-053 (Cmd+P + Cmd+K binding verification)

Assigned: new sub-agent (NAVIGATOR-1).

### W5 — OBSIDIAN-PARITY (persona: HERALD-2 + competitor research)

Use Obsidian as the reference: Properties at top (collapsible, tied to frontmatter), body takes full width, right sidebar has Outline+Backlinks+Tags. Apply to WikiMem page layout. This is the core visual language we need to preserve/restore.

Reference doc: `$PROJECT_ROOT/wikimem/COMPETITOR-UX-2026-04-17.md` §B (Typography & Color) + §F (Editor behavior) + maintainer-prompt-079 explicit Obsidian reference.

New research sub-agent OBSIDIAN-RESEARCH takes `COMPETITOR-UX-2026-04-17.md` + fresh web research on latest Obsidian 1.7+ features (April 2026), delivers a 12-point actionable diff of what wikimem needs to adopt.

Bugs derived: BUG-079-091 through 099 (to be filed by HERALD-2 as research completes).

### W6 — OBSERVER-VIEW (persona: SENTRY — same as W1, follow-up queue)

Once rail-observer button is wired, verify the Observer Experiment History view actually renders correctly. If not: diagnose + fix.

- NOTE-079-060 (Observer view empty on first render)
- Verify "Run Observer Now" SSE stream
- Verify experiment-history SVG timeline renders
- Verify per-run hypothesis/action/result shows

Queued to SENTRY persona as follow-up after W1 completes.

### W7 — QA / VIBE-TEST (persona: ANVIL + CEO)

CEO (this thread) is the continuous vibe tester. Every fix from every sub-agent gets clicked through in Chrome MCP within 60 seconds. If broken: file follow-up bug in the same owner's queue.

ANVIL persona does parallel automated Playwright/Chrome MCP sweeps — one per hour — that click every button and assert no console errors. Report written to `.claude/vp-outputs/anvil-wikimem-report.md`.

## Agent Swarm Spawn Plan

### Phase A (immediately — all parallel) — 7 sub-agents

1. **HERALD-1** (markdown overhaul): git log forensics on `index.html` v0.9.0→v0.10.0, identify regression commits, revert the layout-adds while preserving v0.10.0 backend. Deliver a diff + working fix.
2. **CONCIERGE-1** (connector card E2E + Slack/Discord/Linear modal fixes): follow up on ac8cca8f report. Fix each broken modal. Verify live.
3. **CONCIERGE-MCP-REARCH** (MCP OAuth 2.1 client): new module `src/core/mcp-client/`, wire into connectors page as primary path.
4. **NAVIGATOR-1** (view-switch core): fix showView + tab state + keyboard shortcuts + right-click menu.
5. **OBSIDIAN-RESEARCH** (deep competitor research): Obsidian 1.7+ / Roam / Dex April-2026 state; 12-point actionable diff.
6. **ANVIL-1** (QA sweep): Playwright-driven click-every-button audit with screenshot + console-error capture, first run now, hourly from then on.
7. **SMITH** (harness polish): during lull, improve the .claude skill `/wikimem`, update rules that surfaced (e.g., MCP-first connector architecture as a new rule), write session handoff template.

### Phase B (after W1 ships — ~20 min) — follow-ups on same personas

- SENTRY picks up NOTE-079-060 (Observer view render)
- HERALD-1 picks up TOC rework (BUG-079-077) once metadata consolidated
- CEO continues vibe-testing and queues new bugs discovered

### Phase C (if we hit budget/compaction limits)

- auto-switch.sh kicks in; handoff.md already kept current by CEO
- SuperMemory + Mem0 MCP persist state
- Next session resumes from `.claude/handoff.md` + MASTER-TODOS §49

## Integration Points with Existing Work

- **v0.10.0 code at `$HOME/llmwiki`** — all edits land there (NOT the `$PROJECT_ROOT/wikimem` control-center)
- **MASTER-TODOS.md §49** — single source of truth for all bugs (in `$PROJECT_ROOT/wikimem/`)
- **Maintainer-prompt-079 (raw + structured)** at `$PROJECT_ROOT/wikimem/maintainer-prompts/`
- **Evidence screenshots** at `$PROJECT_ROOT/content/screenshots/wikimem-audit-2026-04-23/`
- **Research** at `$PROJECT_ROOT/resources/read/2026-04-17_mcp_oauth_briefing.md` (read)
- **Competitor UX** at `$PROJECT_ROOT/wikimem/COMPETITOR-UX-2026-04-17.md` (reference)

## Success Criteria (binary — either met or not)

- [ ] Every one of 9 rail buttons clicks to correct view + active-class syncs (BUG-079-001/002/003)
- [ ] Entity page shows ONE metadata presentation, not 5 (BUG-079-070)
- [ ] Content area uses full viewport width (BUG-079-071)
- [ ] Page titles are display titles, not slugs (BUG-079-011)
- [ ] Timestamps humanized, not raw ISO (BUG-079-075)
- [ ] Mentioned-in renders as inline wikilinks (BUG-079-073)
- [ ] TOC in right sticky sidebar or collapsed header (BUG-079-077)
- [ ] Tab state ↔ view state always in sync (BUG-079-020)
- [ ] Connectors page uses MCP OAuth 2.1 client pattern (BUG-079-090)
- [ ] At minimum: Slack/GitHub/Google connectors all work E2E via MCP or bespoke path
- [ ] Zero console errors on 12 surfaces (Home, Files, Search, Graph, Upload, Source Control, Time-Lapse, Connectors, Observer, Settings, Entity page, Concept page)
- [ ] MASTER-TODOS §49 closed: every `[ ]` becomes `[x]` with date + file:line evidence OR justified deferral
- [ ] CEO signs off via screenshot evidence in `$PROJECT_ROOT/content/screenshots/wikimem-audit-2026-04-23/` for each surface

## Risks + Mitigations

| Risk                                                 | Mitigation                                                                                                  |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Sub-agent context degrades mid-task                  | Each sub-agent prompt is self-contained; CEO keeps minimal state; auto-switch.sh + handoff.md + SuperMemory |
| Server restart breaks live audit                     | Only rebuild via `pnpm build` when necessary; hot-reload not configured — accept brief downtime             |
| MCP OAuth 2.1 rearchitecture too big for one session | Phase it: ship the NEW mode additively (keep 44 bespoke cards as fallback) so we can ship intermediate      |
| Git state dirty                                      | CEO keeps diffs small; no force-push; commit at end of each completed workstream with clear message         |
| Maintainer changes priorities mid-session              | CEO pivots immediately, logs the pivot, queues dropped work for next session                                |

## Loop Discipline

CEO is NEVER idle. Between monitoring cycles (when waiting for sub-agent output):

1. Click through a surface we haven't audited yet (Time-Lapse, Graph, Upload pipeline)
2. Add bugs to §49 as found
3. Verify the last sub-agent's change in the browser
4. Update agent queue — push follow-ups to correct persona

Cadence: every 60 seconds CEO either clicks something OR updates a doc OR spawns/supervises an agent.

---

_"You got this, Claude. Use your entire sum of sub-agents and all skills, hooks, MCPs, plugins, and everything else available to you to get this done." — Maintainer the planning sprint, 2026-04-23_
