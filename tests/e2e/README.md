# tests/e2e

End-to-end tests that exercise WikiMem through the CLI and live `wikimem serve` (the
`vibe-audit.mjs` browser flow). Empty for now — e2e coverage is tracked in
`docs/planning/MASTER-TODOS.md`. Unit/integration tests live alongside `src/` and run
via `pnpm test`.
