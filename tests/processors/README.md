# tests/processors

Per-format processor tests (PDF, audio, video, docx, xlsx, URL, …). Empty for now —
processor coverage is tracked in `docs/planning/MASTER-TODOS.md`. The 13-format
ingestion matrix is documented in `README.md` → "13+ Format Ingestion".
